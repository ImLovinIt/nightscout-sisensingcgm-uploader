# cp .env.example .env, fill it in, then: docker compose up -d

services:
  app:
    image: imlovinit1019/nightscout-sisensingcgm-uploader:latest
    # to run your own changes instead of the published image, comment out the
    # image line above and uncomment this one
    # build: .
    container_name: nightscout-sisensingcgm-uploader
    restart: unless-stopped
    env_file: .env
